- name: install pip
  apt:
    name: python3-pip
    state: present

- name: pip install docker-py
  pip:
    name: docker
    extra_args: --break-system-packages

- name: run container
  docker_container:
    name: wartest
    image: "{{ image }}"
    ports: 8084:8080
    state: started
 
